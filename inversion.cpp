#include<iostream>
using namespace std;
count(int *arr,int n)
{
    int X=count(*arr,n/2)
    int Y=count(*arr,)
}
int main()
{
    int n;
    cin>>n;
    int *arr=new int[n];
    for (int i = 0; i < n; i++)
     cin>>arr[i];
    int z=count(*arr,n);
    return 0;    
}